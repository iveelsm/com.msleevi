// @ flow

import React from 'react'

// eslint-disable-next-line
type Props = {
  message: string,
}

// eslint-disable-next-line
const Message = ({ message }: Props) =>
  <p>{message}</p>

export default Message
